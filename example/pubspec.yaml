name: navigation_node_example
description: "NavigationNode, one lesson at a time, with a system back you can press anywhere"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  navigation_node:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  leak_tracker_flutter_testing: ^3.0.8

flutter:
  uses-material-design: true
