# Состояние проекта

> Обновлено: 2026-08-26, перед рестартом. Файл поддерживается агентом и
> переписывается по ходу работы — это снимок «здесь и сейчас», а не история.

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md` и `AGENTS.md`.
   Устройство пакета изнутри — в `docs/architecture.md`, когда понадобится.
   История правок — в `docs/records/`, четыре записи:
   `2026-08-24[1]-post-extraction-review.md` — ревью после переезда, все
   четырнадцать находок с вердиктами; `2026-08-26[1]-observers-design.md` —
   наблюдатели внутри узла: устройство, разобранные альтернативы, что проверено
   пробой; `2026-08-26[2]-observers-review-report.md` — четыре независимых ревью
   той же работы: два дефекта кода, четыре расхождения документов;
   `2026-08-26[3]-delta-review-report.md` — ревью самих правок: слепой ассерт и
   один ложный вердикт предыдущего отчёта;
   `2026-08-26[4]-consumer-doc-review.md` — ревью публичной документации глазами
   потребителя: README перестроен, три переопределения получили дартдоки;
   `2026-08-26[5]-restoration-design.md` — восстановление внутри узла:
   идентификаторов нужно два, и почему.
2. Сверься: `git log --oneline -5`, `git status`.
3. **Готова 0.3.0 и ждёт решения владельца о публикации.** В неё вошло:
   восстановление состояния внутри узла (`restorationScopeId`), перестроенный
   README, дартдоки на три переопределения `NodeNavigatorState` и четыре новых
   урока примера. Неопубликованная 0.2.1 свёрнута сюда же, чтобы у потребителя
   был один релиз с одной записью. 0.2.0 опубликована 2026-08-26, её ревью
   проведено дважды и проработано; `docs/backlog.md` пуст.

   **Незакрытых дефектов нет.** Последний — падение примера на восстановлении —
   найден прогоном настоящего приложения и исправлен (`d0c5046`): билдер
   восстановимого маршрута обязан быть помечен `@pragma('vm:entry-point')`.
   Владельцу отправлено «обнови и проверь», подтверждения от него ещё не было.

   **Открытый вопрос один, и он к соседнему проекту**, ради которого делались
   наблюдатели: их четыре наблюдателя висят на корневом навигаторе, поэтому им
   должно хватить одного `observedFromAbove` по умолчанию — список `observers:`
   нужен, только если между узлом и корнем окажется свой `Navigator` без
   наблюдателей.
4. **Ветка `main`, других веток и worktree нет. Всё запушено.** Проверяй сам:
   `git log --oneline origin/main..HEAD` — должно быть пусто.
5. **CI**: `.github/workflows/ci.yml` — тот же гейт, что и локальный, плюс
   сьюта примера и `git diff --exit-code`, на чистом клоне. Последний
   подтверждённый зелёный прогон был на коммите наблюдателей перед публикацией
   0.2.0; прогоны на более поздних коммитах не проверялись — посмотри сам:
   `gh run list`, `gh run view <id> --log-failed` (`gh` авторизован как
   `vi-k`).
6. **Не под контролем версий:** `.fvmrc` и `.fvm/` (тулчейн заводится
   `fvm use 3.27.0`), корневой `pubspec.lock` — это библиотека, — `build/` и
   `.dart_tool/`. Лок примера, наоборот, закоммичен и пересоздан закреплённым
   тулчейном: на него смотрит шаг «Nothing drifted».

## Где мы сейчас

На pub.dev — **0.2.0, опубликована 2026-08-26**; в репозитории собрана 0.2.1,
документационная, не опубликована. До 0.2.0 были 0.1.0 и 0.1.1, обе
2026-08-24:
[pub.dev/packages/navigation_node](https://pub.dev/packages/navigation_node).

Что в неё вошло — это ревью 2026-08-24 целиком
(`docs/records/2026-08-24[1]-post-extraction-review.md`, четырнадцать находок:
двенадцать исправлено, одна закрыта документацией, одна отклонена). Дефекты:
вечный цикл `popUntil` внутри узла, пропадающее нажатие, когда `canHandlePop`
пришло от чужого навигатора, выключенный узел, глушивший системное «назад»,
устаревшее решение `onPop` после выключения узла, три непокрытых пути отчёта об
ошибке. Прибавка: именованные маршруты внутри узла работают и остаются внутри
него — навигатор узла одалживает таблицу у навигатора сверху. Каждая правка
держится тестом, нагруженность каждого проверена откатом.

**Что в 0.2.0**: наблюдатели внутри узла.
`NavigationNode(observedFromAbove:)` — `true` по умолчанию, узел наследует
наблюдателей навигатора сверху; `NavigationNode(observers:)` называет
наблюдателей для одного узла. Отдать чужие экземпляры навигатору узла нельзя
(Flutter привязывает наблюдателя к одному навигатору и стережёт это
утверждением), поэтому узел вешает свой `_NodeObserver` и пересказывает ему семь
хуков. О собственной странице узел молчит ровно при монтировании. Заказ пришёл
от соседнего проекта; устройство и отклонённые варианты — в
`docs/records/2026-08-26[1]-observers-design.md`, находки четырёх независимых
ревью и вердикты — в `2026-08-26[2]-observers-review-report.md`, второй заход по
самим правкам — в `2026-08-26[3]-delta-review-report.md`.

**Почему 0.2.0, а не 0.1.2**: API правка не ломает, но обновившийся потребитель
начинает кормить свои наблюдатели событиями из узлов, ничего у себя не тронув.
Владелец выбрал номер, который предупреждает сам.

**Известных ограничений, зависящих от нас, не осталось.** Восстановление
внутри узла сделано в 0.3.0 (`restorationScopeId`); в разделе «What a node does
not do» остались `Hero` — это устройство самого Flutter — и `popUntil`,
останавливающийся на странице узла, что не дефект, а следствие того, что узел
себя не опустошает.

**Окно ломающих правок этим закрыто.** До публикации переименовать или убрать
публичное имя стоило ноль; теперь каждое такое изменение стоит версии, и решать
это владельцу.

**Репозиторий обустроен** (2026-08-24): `CLAUDE.md` со
ссылкой на `AGENTS.md`, полный регламент в `AGENTS.md` (восемь разделов),
`docs/backlog.md` для записей владельца, **`docs/architecture.md`** — устройство
узла изнутри: из чего он собран, путь одного нажатия, три вещи, которые легко
сломать обратно. Плюс `dartdoc_options.yaml` (нерезолвящаяся ссылка в дартдоке
— ошибка, а не строка в логе), `.vscode/launch.json` на пример и значки pub и
лицензии в README.

**Гейт §6 пройден целиком 2026-08-26**, после правок по наблюдателям, на полу
3.27.0:

- `fvm flutter test` — **77 тестов**, все зелёные;
- `fvm flutter analyze` — 0 issues (корень обходит и `example/`);
- `fvm dart format --set-exit-if-changed lib test` — 0 changed (4 файла);
- `fvm dart doc --dry-run` — 0 warnings, 0 errors, уже со строгим
  `dartdoc_options.yaml`;
- `fvm dart pub publish --dry-run` — 0 warnings, архив ~34 КБ (на грязном
  дереве он же даёт одно: «checked-in files are modified in git» — это про
  незакоммиченное, а не про пакет);
- `sh docs/ru/check.sh` — переводы актуальны: 2;
- пример: `analyze` чистый, **28 тестов** зелёные.

## Найдено по пути и не чинилось

**`pushReplacement` и `pushAndRemoveUntil((_) => false)` на первой странице узла
падают** на ассерте фреймворка «A page-based route cannot be completed using
imperative api»: страница узла page-based, а защищён от этого только `popUntil`
— переопределением в `NodeNavigatorState`. В README и дартдоках об этом ни
слова. Нашлось ревью наблюдателей 2026-08-26, к нему отношения не имеет.

**`.github/workflows/ci.yml` ссылается на «`AGENTS.md` §4»**, а гейт — §6.

## Чего здешние тесты поймать не могут

**Зелёная сьюта не доказывает, что пример работает.** Тестовая VM собирает
функцию по имени и без `@pragma('vm:entry-point')`, а настоящее приложение —
нет: восстановимый маршрут падал в движке при зелёном гейте. Если трогаешь
восстановление или что-нибудь ещё, что фреймворк достаёт по имени, **проверяй
запуском**, а не прогоном.

Как это делать, не пачкая рабочее дерево чужим SDK: `git clone . <куда-то>`,
и уже там `flutter run -d macos`. Приложение можно заставить нажимать на себя
само — таймер в `initState` урока, — и читать консоль из лога. Так найден
`d0c5046`; разбор — в `docs/records/2026-08-26[5]-restoration-design.md`.

**И постановка опыта решает.** Тот же урок, открытый стартовым экраном, был
чист; открытый через список — падал с первого нажатия. Воспроизводить надо тот
путь, которым ходит человек.

## Грабли, на которые не стоит наступать второй раз

**Перехватил `FlutterError.onError` — передай дальше.** Тест, который читает
`FlutterErrorDetails`, ставит свой обработчик; если не позвать из него прежний,
биндинг не получает ошибку, `tester.takeException()` возвращает `null`, а
`pumpAndSettle` ждёт своего десятиминутного таймаута вместо двух секунд. Так и
выглядело: прогон висел ровно 10:01. Пишется так:

```dart
final previous = FlutterError.onError;
FlutterError.onError = (details) {
  reported.add(details);
  previous?.call(details);
};
addTearDown(() => FlutterError.onError = previous);
```

**У `macOS` нет `timeout`.** `timeout 240 fvm flutter test` кончается кодом
127 и пустым выводом — это «команда не найдена», а не упавший прогон.

## Проверки

Команды — в `AGENTS.md` §6. Числа — выше. Правка, тронувшая только
`docs/handoff.md`, `docs/backlog.md` или `docs/records/`, гейта не касается:
эти файлы ни одна из семи команд не читает. Тулчейн: Flutter 3.27.0 через fvm;
`.fvmrc` не под контролем версий — заводится `fvm use 3.27.0`.
