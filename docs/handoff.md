# Состояние проекта

> Обновлено: 2026-08-24. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история.

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `AGENTS.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. **Очереди работ нет.** Открыто одно решение владельца — публиковать ли
   **0.1.1**: она подготовлена и лежит в `main`, на pub.dev сейчас 0.1.0.

## Откуда взялся этот репозиторий

Пакет жил внутри [scopo](https://github.com/vi-k/scopo) до её версии 0.11.0 и
вынесен 2026-08-24 по находке U7 её потребительского ревью
(`docs/records/2026-08-23[1]-consumer-review.md` там же). Причина: он не
импортирует ничего, кроме Flutter, внутри scopo им не пользовалось ничто, а
человек, ищущий вложенный навигатор, не нашёл бы его в пакете про
dependency injection. Измерение и цена вариантов — в записи
`docs/records/2026-08-24[1]-navigation-node-boundary-design.md` репозитория
scopo.

Код, сьюта и пример перенесены **без изменений поведения**. Изменилось только
то, что связывало их со scopo:

- `{@category utils}` снят с трёх публичных объявлений — тема `utils` осталась
  в scopo;
- у `PreviousNavigatorExtension` появился свой дартдок: раньше вместо него
  стояла одна строка `{@category utils}`, и без неё `public_member_api_docs`
  краснел;
- сьюта клала над узлом `ScopeModel<_Config>`, чтобы проверять, видит ли
  маршрут то, что лежит над экраном. Теперь это `_ConfigScope` —
  `InheritedWidget` в самом файле теста, с чтением через
  `getInheritedWidgetOfExactType` (это ровно прежний `listen: false`);
- в примере `ScreenScope` был `ScopeModel<Ticket>`, стал `InheritedWidget`,
  несущий `Ticket`; `main()` больше не ставит `ScopeConfig.observer`.

**Ссылок на scopo в пакете не осталось нигде**, кроме двух намеренных: раздел
README «Where it comes from» и ссылка на `scopo_demo` из README примера.

## Где мы сейчас

Версия **0.1.0 опубликована на pub.dev 2026-08-24** — первая версия пакета,
[pub.dev/packages/navigation_node](https://pub.dev/packages/navigation_node).
**0.1.1 подготовлена и не опубликована:** в ней одна правка — отчёт об ошибке
из узла называл библиотеку `scopo`, теперь `navigation_node`. Публиковать — по
явной просьбе владельца (`AGENTS.md` §5).

**Окно ломающих правок этим закрыто.** До публикации переименовать или убрать
публичное имя стоило ноль; теперь каждое такое изменение стоит версии, и решать
это владельцу.

**Репозиторий обустроен по образцу scopo** (2026-08-24): `CLAUDE.md` со
ссылкой на `AGENTS.md`, полный регламент в `AGENTS.md` (восемь разделов),
`docs/backlog.md` для записей владельца, **`docs/architecture.md`** — устройство
узла изнутри: из чего он собран, путь одного нажатия, три вещи, которые легко
сломать обратно. Плюс `dartdoc_options.yaml` (нерезолвящаяся ссылка в дартдоке
— ошибка, а не строка в логе), `.vscode/launch.json` на пример и значки pub и
лицензии в README.

**Гейт §6 пройден целиком 2026-08-24** на полу 3.27.0:

- `fvm flutter test` — **44 теста**, все зелёные;
- `fvm flutter analyze` — 0 issues (корень обходит и `example/`);
- `fvm dart format --set-exit-if-changed lib test` — 0 changed (4 файла);
- `fvm dart doc --dry-run` — 0 warnings, 0 errors;
- `fvm dart pub publish --dry-run` — 0 warnings, архив ~30 КБ;
- `sh docs/ru/check.sh` — переводы актуальны: 2;
- `fvm dart doc --dry-run` — 0 warnings, 0 errors, уже со строгим
  `dartdoc_options.yaml`;
- пример: `analyze` чистый, **17 тестов** зелёные.

## Что связано со scopo и ждёт публикации

`example/scopo_demo` в репозитории scopo показывает узел в паре со скоупом и
потому зависит от этого пакета — **с pub.dev, `^0.1.0`**, с того же дня.
Git-мостик, стоявший там до публикации, снят.

**scopo 0.11.0 ещё не опубликована**, и её `CHANGELOG` — единственное место,
которое расскажет потребителю, куда делся `NavigationNode`. Пока её нет, на
pub.dev стоит scopo 0.10.0, в которой виджет ещё внутри.

## Проверки

Команды — в `AGENTS.md` §4. Числа — выше. Тулчейн: Flutter 3.27.0 через fvm;
`.fvmrc` не под контролем версий, как и в scopo, — заводится `fvm use 3.27.0`.
