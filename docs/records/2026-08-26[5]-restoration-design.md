# Восстановление внутри узла: почему одного идентификатора мало

> **Состояние: сделано, вошло в 0.3.0** (не опубликована). Устройство
> `NavigationNode(restorationScopeId:)`, находка пробы — идентификаторов нужно
> два — и то, чего мы намеренно не обещаем. Закрывает последний пункт из
> «What a node does not do», зависевший от нас; из той же семьи, что находка R7
> ревью `2026-08-24[1]-post-extraction-review.md`.

## Откуда взялось

Соседний проект восстановления не просил и не пользуется им нигде — так и
записано в `2026-08-26[1]-observers-design.md`. Взялись по просьбе владельца,
уже после публикации 0.2.0.

## Что показала проба

Пробным тестом, который потом удалён, на полу 3.27.0. Проверялись шесть случаев.

**Пробросить `restorationScopeId` навигатору узла — недостаточно.** Стек не
возвращался:

```
B before: true    B after: false
```

**Не хватало второго идентификатора — у собственной страницы узла.** Маршрут,
запушенный императивно, хранится прикреплённым к той странице, над которой
лежит, а страница без `restorationId` в восстановлении не участвует вовсе.
Проставили — вернулось:

```
B before: true    B after: true
C before: true    C after: true
```

**C — это `restorablePushNamed` через одолженную таблицу маршрутов.** Две
возможности работают вместе, и это не было очевидно: имя строится заново уже
после перезапуска, то есть таблицу приходится одалживать второй раз.

**Два узла с одним идентификатором** ловятся фреймворком громко:
`Multiple owners claimed child RestorationBuckets with the same IDs`. Молчаливой
порчи данных нет, и уникальность — забота приложения, ровно как с `enabled`.

**Два узла с разными идентификаторами** восстанавливаются каждый свой.

**Приложение, не включавшее восстановление**, ломает только тестовый харнесс:
`restartAndRestore()` отказывается работать без корневого бакета. В настоящем
приложении имя узла в этом случае просто не используется — как и
`restorationScopeId` любого `Navigator`.

## Что сделано

Один необязательный параметр, уходящий в два места: навигатору узла и его
собственной странице. Идентификатор страницы — константа `'node'`: её бакет
живёт **внутри** бакета навигатора, поэтому сталкиваться ему не с чем, что
проба и подтвердила (падает идентификатор навигатора, не страницы).

`_pages` пересобирается ещё и тогда, когда `restorationScopeId` появился или
исчез: список страниц — поле, а страница несёт этот идентификатор в себе.

## Чего мы намеренно не обещаем

**Обычный `push` не восстанавливается никогда.** Он несёт замыкание, и это
правило Flutter, а не узла. Если написать «стек внутри узла восстанавливается»
без этой оговорки, получится ровно та беда, которую в этом пакете чинили дважды:
документ шире кода. Поэтому в README, в зеркале, в дартдоке и в CHANGELOG
названы именно `restorablePush` и его соседи.

**Уникальность имён — на приложении.** Узел не может её обеспечить и не
пытается: у него нет способа узнать про соседей больше, чем есть у `enabled`.

## Нагруженность

Каждая половина правки откачена по отдельности, тест назван:

- снят `restorationScopeId` у навигатора → падает «a node restores the stack
  pushed inside it»;
- снят `restorationId` у страницы → падает он же и «a name pushed restorably
  inside a node comes back».

Контрольный тест держит прежнее поведение: без идентификатора узел по-прежнему
начинает с собственной страницы.

## Урок 10: как показать восстановление там, где платформы нет

Владелец спросил, нельзя ли увидеть это на Mac. Нельзя средствами системы:
восстановление приходит из платформы по каналу `flutter/restoration`, а macOS
ни блоба не хранит, ни приложение не убивает. Но платформа — не единственный
источник, и в примере теперь стоит `PocketRestorationManager`: он переопределяет
`initChannels` (движка нет), ловит блоб в `sendToEngine` и отдаёт его обратно
через `handleRestorationUpdateFromEngine` — тот самый вызов, который делает
движок после настоящего перезапуска. Все три точки расширения помечены
`@protected` и задокументированы фреймворком как предназначенные для подмены.

Проба нашла две вещи, без которых не работает:

- **`RootRestorationScope` читает менеджер биндинга**, а не ближайшего предка.
  То есть `MaterialApp.restorationScopeId` карманный менеджер не увидит никогда
  — `UnmanagedRestorationScope` приходится вешать прямо на узел;
- **снимок блоба надо брать до разбора стека.** Сериализация отложена в
  post-frame callback, поэтому в самом уроке порядок сейчас ни на что не влияет
  — но стоит между попами появиться кадру, и снимок окажется пустым. В коде
  урока это записано именно так, а не как «иначе не работает»: первая редакция
  комментария утверждала второе, и проверка откатом это поймала.

Что урок честно называет притворством: умирание. Настоящие здесь — данные и
дорога, которой они возвращаются.

**И одна ошибка, которую первая редакция урока успела сделать.** Кнопка
разбирала стек живого навигатора (`popUntil`) и тут же подсовывала ему новые
данные. Нажатие **во время анимации пуша** роняло ассерт фреймворка
`navigator.dart:3348` — маршрут уничтожали под тем, который ещё приезжал.
Настоящий перезапуск так не делает: процесс умирает целиком. Теперь кнопка
уничтожает всё дерево ниже (новый «век» узла и новый менеджер, засеянный
сохранёнными байтами) — это и честнее, и не ломается. Держит тест «the button is
safe while a route is still arriving»; уберёшь пересоздание — краснеет.

**И вторая, из той же кнопки.** Первая редакция починки пересоздавала менеджер и
уничтожала старый на месте. Уничтоженный менеджер может держать **уже
запланированный post-frame callback сериализации**: тот срабатывает позже, идёт
за данными в бакет, которого больше нет, и роняет кадр. Воспроизвести тестом не
удалось — доказательства нет, — но узор опасен по построению, и его убрали:
менеджер у урока один на всю жизнь, а «убийство» пересоздаёт дерево и отдаёт
байты **тому же** менеджеру. Так делает и устройство, и фреймворк сам меняет
бакет в правильном порядке: сначала раздаёт новый, потом уничтожает старый.

**Как читать этот ассерт вообще.** `schedulerPhase == SchedulerPhase.idle` в
`handleBeginFrame` означает, что кадр пришёл из движка, когда планировщик не был
в покое, — то есть **предыдущий кадр не завершился**: из него вылетело
исключение. Настоящая ошибка в консоли стоит выше этой, и искать надо её.

Нашлось это не из кода, а прогоном: владелец принёс ассерт со **stable/3.44.4**
(`schedulerPhase == SchedulerPhase.idle`, `binding.dart:1253`), которого на нашем
полу нет по номеру строки. Воспроизвести его один в один на 3.27.0 не удалось —
воспроизвелось другое падение той же кнопки в том же сценарии, и оно исправлено.

## Что на самом деле ломалось у владельца

Три раза подряд приходил ассерт
`schedulerPhase == SchedulerPhase.idle` (`binding.dart:1253`, stable 3.47.0), и
три раза я чинил не то. Разбор по шагам, потому что метод оказался важнее
находки.

**Ассерт вторичен.** `handleBeginFrame` требует покоя планировщика: движок
прислал кадр, когда предыдущий не закончился. Искать надо не его.

**Постановка опыта решала всё.** Первый мой прогон открывал урок 10 **стартовым
экраном** — и был чист. Владелец же входит в урок **через список**, то есть узел
живёт внутри запушенного маршрута. С этим исправлением постановки всё
воспроизвелось с первого нажатия.

**Первая строка лога — вот она:**

```
ERROR: To closurize 'l10_restoration.dart::_restorableDetails' from native code,
       it must be annotated.
```

`Navigator.restorablePush` требует, чтобы билдер был статической функцией с
`@pragma('vm:entry-point')` — это написано в его же документации. Навигатор
спрашивает функцию **по имени**, когда строит маршрут заново, и имя должно
пережить компилятор. Без пометки восстановление падает внутри движка, кадр
разъезжается — отсюда и ассерт про планировщик.

**Почему сьюта молчала:** тестовая VM находит функцию по имени и без пометки. То
есть зелёный гейт тут не значил ничего, и это стоит помнить: у пакета есть класс
ошибок, который тесты по устройству поймать не могут.

**Вердикт: исправлено.** Прагма стоит и в уроке, и в тестовом помощнике пакета,
а требование записано в README, в зеркале и в дартдоке `restorationScopeId` —
раньше мы советовали `restorablePush`, умалчивая об условии. Проверено прогоном
настоящего приложения на stable 3.47.0: до пометки 4 ошибки closurize, 4
ассерта, 38 `markNeedsBuild`; после — ноль, ноль и ноль, шесть кругов подряд.

Повторено владельцем 2026-08-27 **на объявленном полу 3.27.0**, `fvm flutter
run -d macos`, вход через список: шесть кругов restorable и два ordinary,
«Pushed restorably» возвращается, «Pushed the ordinary way» нет, в логе ноль по
всем трём словам. То есть правка держится не только на той stable, где дефект
нашёлся, но и на версии, под которую пакет объявлен.

**Чего я по дороге наговорил лишнего**, и что стоит помнить как урок: заявил,
что «hot reload виноват» (не виноват — воспроизводится и без него); заявил, что
одна правка убрала ошибки, опираясь на **один** прогон, — повтор того же кода
дал четыре ассерта, то есть первый ноль был везением. Правило простое: один
зелёный прогон флаки-сценария не доказывает ничего.

## Грабли, найденные по дороге

**Leak-трекер и `restartAndRestore`.** Тесты восстановления оставляют после себя
`TestRestorationManager` и `RestorationBucket` — объекты тестового биндинга,
которые пакет не создаёт и не может утилизировать. Полная сьюта при этом
зелёная (их подчищает биндинг следующего теста), а вот **один такой тест,
запущенный отдельно, роняет `tearDownAll`** — и это сбивает с толку сильнее,
чем стоит. Оба класса перечислены поимённо в `experimentalLeakTesting` этих
четырёх тестов, а не заглушены целиком: утечка чего-нибудь нашего в них
по-прежнему будет поймана.
